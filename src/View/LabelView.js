import React from "react";
import { Outlet } from "react-router-dom";

const LabelView = () => {
	return (
		<>
			<>labelView</>
			{/* ShowMemoWithLabel */}
			<Outlet />
		</>
	);
};

export default LabelView;
